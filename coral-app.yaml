name: coral-app
region: nyc
services:
- build_command: yarn build
  environment_slug: php
  github:
    branch: main
    deploy_on_push: true
    repo: htmlshaman1/twig
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  name: twig
  routes:
  - path: /public/index.php
  run_command: heroku-php-apache2
  source_dir: /
